
public class MyString {
	
	/*
	 * length("Java")
	 * -> (1 + length( "ava" ))
	 * -> (1 + (1 + length( "va" )))
	 * -> (1 + (1 + (1 + length( "a" ))))
	 * -> (1 + (1 + (1 + (1 + length( "" )))))
	 * -> (1 + (1 + (1 + (1 + 0))))
	 * -> 4
	 */
	public static int length(String str) {
		//TODO base case
		
		// TODO recursive case
		
		return -1; // dummy return value
	}
	
}
